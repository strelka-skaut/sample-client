{
  "dependencies": {
    "@strelka-skaut/js-api-spec": "^1.0.6",
    "xhr2": "^0.2.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:tsc",
    "build:tsc": "tsc"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
